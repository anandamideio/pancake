import ArrayStack = require('./dist/dataStructures/arrayStack');
import Stack = require('./dist/dataStructures/stack')
import Queue = require('./dist/dataStructures/queue')

export = {
  ArrayStack,
  Stack,
  Queue,
};
